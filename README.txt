NodeTest-1.3

See https://github.com/sdsanft/NodeTest-1.2

A web application built using NodeJS and Express that runs on a local server

Working to save data using MongoDB

To set up the project run
    npm install
in the command line

To launch the server run
    node index.js
in the command line

Edit index.js to change the port
